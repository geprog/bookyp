apiVersion: v1
kind: Service
metadata:
  name: "{{ include "bookyp.fullname" . }}-frontend"
  labels:
    {{- include "bookyp.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bookyp.selectorLabelsFrontend" . | nindent 4 }}
